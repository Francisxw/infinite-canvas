# Archived QA scripts

This directory contains older `qa_verify` revisions preserved for history.

`../qa_verify.py` is the current top-level entry point. Use the archived variants only when you need to compare historical manual QA flows.
