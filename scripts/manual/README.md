# Manual scripts

This directory keeps one-off debugging, exploratory, and migration verification scripts that were previously scattered across the repo root and `frontend/`.

Most of these scripts still contain historical absolute paths or old local ports, so treat them as references first and update them before executing.
