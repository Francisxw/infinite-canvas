# Scripts

- `qa_verify.py`: current manual QA smoke script. Its screenshots are written to `artifacts/qa_verify/`.
- `manual/`: one-off exploratory and debugging scripts kept for reference. Many still contain legacy local paths or ports and may need adjustment before re-running.
- `archive/`: older `qa_verify` revisions retained as historical reference.
